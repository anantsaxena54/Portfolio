server.port=${PORT:8080}
spring.application.name=anant-portfolio

# Resend API (REST-based email — works on Render free tier)
resend.api.key=${RESEND_API_KEY:}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# Contact email (where you receive messages)
portfolio.contact.email=${PORTFOLIO_CONTACT_EMAIL:anantsaxena54@gmail.com}
